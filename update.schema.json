{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "72flasher Update Manifest",
  "description": "Schema for the 72flasher application update manifest.",
  "type": "object",
  "properties": {
    "latest_version": {
      "type": "string",
      "description": "The latest available version in semver format (e.g. 1.0.1).",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "download_url": {
      "type": "string",
      "description": "The URL to download the latest version.",
      "format": "uri",
      "pattern": "^https?://"
    }
  },
  "required": ["latest_version", "download_url"],
  "additionalProperties": false
}
